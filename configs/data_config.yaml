# Data and results
result_dir: "./results"
data_loading_arguments:
  use_curated_dataset: false  # use only the dataset from 2024 with 50 samples
  add_curated_dataset: true  # add the dataset from 2024 with 50 samples
  remove_samples_without_label: false
  sample_small_dataset: true
  min_samples_per_class: 15  # only used is sample_small_dataset is true
  save_chunk_size: 50  # update csv results every this number of samples